# Merge these listener settings into the existing broker configuration.
# Per-listener mode preserves existing anonymous listeners.
per_listener_settings true

listener 1884
protocol mqtt
allow_anonymous false
password_file /mosquitto/config/camdash-passwords
acl_file /mosquitto/config/camdash-acl

